#include <stdio.h>
#include "main.h"


/**
 * mul - Multiplies two integers
 * @k: The first number to multiply
 * @n: The second number to multiply with first number
 * Return: Multiplication between k and n
 */

int mul(int k, int n)

{

	return (k * y);

}
